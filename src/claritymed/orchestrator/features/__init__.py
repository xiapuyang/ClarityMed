"""Orchestrator feature plugins.

Each plugin bundles a coherent capability (ingest tools, knowledge
retrieval, vision predict, …) that the orchestrator wires into the
agent's toolset at construction time.
"""
