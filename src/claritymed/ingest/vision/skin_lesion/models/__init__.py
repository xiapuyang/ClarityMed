"""Skin lesion model specs.

Add a new architecture (e.g. EfficientNet-only, ViT, etc.) by dropping a
new ``models/<name>.py`` module here that exports a
:class:`~claritymed.ingest.vision.forge.spec.ModelSpec`.
"""
