"""Per-architecture :class:`ModelSpec` instances for the BUSI dataset.

Each module exports one :class:`~claritymed.ingest.vision.forge.spec.ModelSpec`
constant. The forge CLI loads them via dotted-path:ATTR selector.
"""
